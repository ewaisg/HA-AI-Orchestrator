# LOC-009 cache-safe update repair and repository resume

Date: 2026-09-09
Status: REVIEW — automated checks and independent code review complete; live update pending.

## Review and bounded scope

Starting checkout was clean on main at `a330c9c`. Contrary to the stale tracker,
`b0b9a7e` already implemented cache hashing and `a330c9c` added the initial
workflow schema. The workflow schema is not connected to an executable runtime
or UI, and its prerequisite release gate remains open.

Primary claimed LOC-009; backend subagent owned only `panel.py` and
`test_panel.py`; an independent read-only reviewer inspected both the existing
implementation and the repair. Primary owned tracker, evidence and installation
guidance. No provider contract, action permission, or live configuration changed.

The repair moves bundle filesystem reads out of Home Assistant's event loop
using its executor and calculates one URL per registration. Existing-panel
validation reuses that URL. Tests now replace actual temporary bundle bytes,
verify unchanged-byte stability, exercise a missing-file fallback, and check
that initial registration and compatibility validation read off the event-loop
thread. The independent reviewer approved this repair with no actionable
findings, conditional on automated and live evidence being recorded separately.

## Observed verification

Logs are retained locally under `artifacts/review-2026-09-09/` (ignored).
Counts overlap and must not be summed.

| Check | Result | Log |
|---|---|---|
| `uv run python scripts/run_pure_tests.py` | 234 passed; 5 dependency deprecation warnings | `pure.txt` |
| `npm --prefix frontend run check` | 116 tests across 9 files; scripts, lint, typecheck, build, sync and identity passed | `frontend.txt` |
| Core 2026.9.0 focused panel suite | 12 passed | `cache-panel-202690.log` |
| Core 2026.9.0 full pytest suite | 383 passed | `full-backend-202690.log` |
| Core 2026.8.3 full pytest suite | 383 passed | `full-backend-202683.log` |
| `uv run ruff check .` | Exit 0, all checks passed | Tool output |
| `uv run python scripts/canary_scan.py` | Exit 0, no findings | Tool output |
| `git diff --check` | Exit 0 | Tool output |

Home Assistant tests ran from an isolated WSL Ubuntu source snapshot at
`~/.local/share/ha-ai-orchestrator/candidate-loc009-20260909`, using the existing
`venv-202683` and `venv-202690` sibling environments. Snapshot archive:
`artifacts/review-2026-09-09/cache-fix-source.tar`. Commands from that snapshot
use the respective environment Python with `-m pytest -q`, or
`-m pytest tests/home_assistant/test_panel.py -q` for the focused suite.
The focused runner emitted path-normalization warnings for `sys.prefix` and
`sys.exec_prefix`; tests completed. A preliminary Windows Home Assistant test
attempt could not collect because its plugin requires Unix `fcntl`; Linux
results above provide the actual integration verification.

Frontend bundle remains 95,427 bytes, SHA-256
`d24d6b50aa09cf805822f7ec06af94bfdd84813c153e700d328003701677dce2`.
No frontend behavior was changed this session.

## Concrete candidate and remaining acceptance

Complete 18-file integration archive: `outputs/loc-009/ai-orchestrator-cache-safe.zip`.
Archive SHA-256: `658e1f81a8b21b4f0818234d4f0376520f66f67e9cbeab0e3e6b866bac3f54ed`.
Per-file SHA-256 manifest: `outputs/loc-009/identity.json`. Packaging normalizes
text to LF and includes the complete tracked integration directory. A direct
archive comparison confirmed all 18 packaged integration files match the
Linux-tested snapshot after LF normalization.

No live installation, restart, or service-worker observation was performed in
this session. LOC-009 remains REVIEW, and ENV-013 records the unknown.
Automatic registration uses the content hash; the manual YAML fallback remains
unversioned, with cache recovery documented in installation guidance.

Resume at installation of this complete candidate, then observe its hashed
module URL and panel/chat behavior without manually clearing caches. Retain
redacted evidence of the actual fetched bundle and working chat. Obtain LOC-008
Android keyboard/transcript/spacing confirmation, and complete the remaining
LOC-003 timeout/cancellation, LOC-004 duplicate/transport, and LOC-005 registry
mutation checks before LOC-007. Earlier recorded live chat success is preserved;
no new live success or completed Phase 1 release is claimed here.

## Owner live acceptance after candidate handoff

The owner explicitly confirmed: "Updated panel/chat worked without clearing
caches" in response to the scoped acceptance question. This closes the named
LOC-009 live update behavior together with the automated changed-byte tests
and independent review above. LOC-009 is DONE. This is owner-reported evidence,
not an agent-observed network trace; no exact live URL or fresh Core version
is inferred. Android and other Phase 1 acceptance items remain separate.
