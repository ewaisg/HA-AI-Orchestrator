# WFL-001 schema validation and migration verification

Date: 2026-09-09. Status: REVIEW; scoped code and independent verification complete,
prerequisite LOC-007 remains open. No workflow runtime or live action is released.

Primary owned workflow_schema.py and docs. A test subagent owned the previously
empty tests/quality/test_workflow_schema.py. An independent reviewer reproduced
defects before the repair, reviewed the repair and tests, and approved the pure
schema scope with no remaining actionable findings.

## Changes

- Conditions and steps reject fields belonging to other kinds instead of dropping them.
- Notification references must be in the notify namespace; registry existence and
  runtime authorization are separate requirements, not inferred by this parser.
- Versions require an exact integer, identifiers match fully, and HH:MM is ASCII.
- Huge numeric bounds raise WorkflowValidationError rather than OverflowError.
- Unsupported keys never echo caller-controlled text into validation errors.
- Documentation distinguishes stored enabled state from future import approval.
- Added 72 tests covering valid kinds, roundtrips, migration idempotence/input
  preservation, future-version rejection, malformed types, limits and regressions.

## Verification

| Command/check | Observed result |
|---|---|
| Isolated schema pytest, plugin autoload disabled with pytest_asyncio.plugin | 72 passed; independently rerun, 72 passed |
| uv run python scripts/run_pure_tests.py | 306 passed, 5 dependency warnings |
| WSL Core 2026.9.0 full python -m pytest -q | 455 passed in 4.06s |
| WSL Core 2026.8.3 full python -m pytest -q | 455 passed in 3.99s |
| uv run ruff check . | All checks passed |
| uv run python scripts/canary_scan.py | Exit 0, no findings |
| git diff --check | Exit 0; Windows line-ending notice only |

Local ignored logs: artifacts/review-2026-09-09/workflow-pure.txt,
workflow-backend-202690.txt and workflow-backend-202683.txt. Linux full runs used
~/.local/share/ha-ai-orchestrator/candidate-loc009-20260909 with the repaired
schema/test files copied from this checkout and the corresponding persistent
venv-202690 / venv-202683 Python. No frontend code changed or frontend test
rerun was required for this pure schema change.

Independent reviewer additionally reported all 10 original repros safely rejected
and 210 malformed field mutations without unexpected exceptions. Reviewed raw
SHA-256: source 4ec1186cd1ab3080501b6c050c38a1f8be868a1b24c33c9e8918ac279ece13d6;
tests b4d5bcdae46f31567d7329a25d96641419b925eda9a8ce4078cb9ebeca3dcf68.

## Resume

LOC-009 is now accepted by the owner separately. Close LOC-008 Android layout
and LOC-003/004/005 remaining acceptance, then LOC-007. WFL-001 code/test review
is ready; runtime work proceeds under WFL-002 after the prerequisite gate.
Storage, discovery, exact action authorization and live notification evidence
remain unimplemented. See ../architecture/WORKFLOW-SCHEMA.md for the contract.
